<div id="flash-messages" class="container">
  <% flash.each do |type, message| %>

    <div class="alert alert-<%= type == 'notice' ? 'primary' : 'danger' %>">
      <%= message %>
    </div>
  <% end %>
</div>
