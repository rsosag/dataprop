<main class="container">
  <h1><%= t('new_property') %></h1>
  <%= form_with model: @own_property, url: own_properties_path, method: :post, data: {turbo: false} do |f| %>
    <%= render 'form', f: f %>
  <% end %>
</main>
