<main class="container">
  <h1><%= t('edit_property') %></h1>
  <%= form_with model: @own_property, url: own_property_path, method: :put, data: { turbo: false } do |f| %>
    <%= render 'form', f: f %>
  <% end %>
</main>
